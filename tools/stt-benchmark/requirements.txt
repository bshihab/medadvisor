# Core
jiwer>=3.0            # WER metric
soundfile>=0.12       # read WAVs
numpy>=1.24

# Transcription engines (Apple Silicon)
mlx-whisper>=0.4      # Whisper on MLX
parakeet-mlx>=0.2     # NVIDIA Parakeet on MLX

# LLM (speaker attribution) — MedGemma via MLX (prebuilt wheels, no compile).
# Same model family as the app; runtime differs (MLX here vs llama.cpp in-app),
# which is fine for measuring attribution accuracy.
mlx-lm>=0.20
